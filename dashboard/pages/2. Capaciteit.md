---
title:
hide_title: true
hide_header: false
breadcrumb: false
hide_toc: true
---

<Dropdown
    name=kamer
    data={kamerSelector}
    value=kamer
    multiple=true
    selectAllByDefault=true
/>


<Grid cols=3>
  <BigValue
    data={bigvalues}
    value=maxBenutting
    title="Maximale Benutting"
    fmt=pct
  />
  <BigValue
    data={bigvalues}
    value=maxBehandeltijd
    title="Maximale Behandeltijd (min)"
  />
  <BigValue
    data={bigvalues}
    value=maxWachttijd
    title="Maximale Wachttijd (min)"
  />
</Grid>

<Grid cols=3>

  <BigValue
    data={bigvalues}
    value=minBenutting
    title="Minimale Benutting"
    fmt=pct
  />
  <BigValue
    data={bigvalues}
    value=minBehandeltijd
    title="Minimale Behandeltijd (min)"
  />
  <BigValue
    data={bigvalues}
    value=minWachttijd
    title="Minimale Wachttijd (min)"
  />


</Grid>

<LineChart
  data={capaciteit}
  x=maand
  y=benuttingPerc
  title="Benutting per kamer (% van beschikbare capaciteit)"
  yAxisTitle="Benutting (%)"
  series=kamer
  yFmt=pct
  markers=true
  markerShape=emptyCircle
/>

<LineChart
  data={capaciteit}
  x=maand
  y=gemiddeldeBehandeltijdPerPatientMin
  title="Gemiddelde Behandeltijd per patient"
  yAxisTitle="Behandeltijd (min)"
  series=kamer
  markers=true
  markerShape=emptyCircle
/>

<LineChart
  data={capaciteit}
  x=maand
  y=gemiddeldeWachttijdPerPatientMin
  title="Gemiddelde Wachttijd per patient"
  yAxisTitle="Wachttijd (min)"
  series=kamer
  markers=true
  markerShape=emptyCircle
/>

<DataTable data={capaciteit} title="Capaciteit" />

```sql kamerSelector
select distinct kamer
  from hospital.capaciteit
```

```sql capaciteit
select *
  from hospital.capaciteit
 where kamer in ${inputs.kamer.value}

```

```sql bigvalues
select max(benuttingPerc) maxBenutting,
       max(gemiddeldeBehandeltijdPerPatientMin) maxBehandeltijd,
       max(gemiddeldeWachttijdPerPatientMin) maxWachttijd,
       min(benuttingPerc) minBenutting,
       min(gemiddeldeBehandeltijdPerPatientMin) minBehandeltijd,
       min(gemiddeldeWachttijdPerPatientMin) minWachttijd
  from hospital.capaciteit
```